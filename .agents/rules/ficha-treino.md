---
trigger: always_on
---

Use exclusivamente React 19 e Tailwind CSS 4.

O gerenciamento de estado deve ser feito com Zustand.

Todos os dados devem ser persistidos via localStorage (Zustand persist), não utilize backend em nuvem.

Priorize interface mobile-first, com botões e áreas de clique grandes (mínimo 44px).